# Sendfile UI

An interface for uploading / downloading a file.

## Development

This app was created with `create-react-app`, which encapsulates a lot of complexity.

The sendfile-ui package is UI around the client logic in the sendfile pacakge.
