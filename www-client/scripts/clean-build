#!/usr/bin/env bash
set -e
set -x

REPO_ROOT=$(git rev-parse --show-toplevel)
cd $REPO_ROOT

rm -fr rust/pkg
wasm-pack build --release --target bundler rust

rm -fr sendfile/dist
rm -fr sendfile/node_modules
(cd sendfile && yarn install && yarn tsc)

rm -fr sendfile-ui/build
rm -fr sendfile-ui/node_modules

# Build for deploy
(cd sendfile-ui && yarn install && yarn build)
# Compress for deploy.
# NOTE: we keep (-k) the original for the weirdos that don't support gzip and
# for our nginx configuration, which will serve up our SPA index whenever
# requesting a URL that doesn't exist on disk.
(cd sendfile-ui/build && find . -type f -name \*.wasm | xargs gzip -k -9)

# Serve for dev
# (cd sendfile-ui && yarn install && yarn start)
