#!/usr/bin/env bash
set -e

WWW_CLIENT=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)

(cd $WWW_CLIENT/glopfile-ui && yarn prettier --write .)
(cd $WWW_CLIENT/glopfile && yarn prettier --write .)
