# The relay this client talks to. Overridden by .env.development when running
# `yarn start`, so a local checkout points at a local relay.
REACT_APP_GLOPFILE_API_ENDPOINT=https://api.s.endoftheworl.de
